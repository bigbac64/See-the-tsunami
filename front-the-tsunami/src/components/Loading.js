import React from 'react';

/**
 * Composant servant à afficher une animation de temps de chargement
 * @returns {JSX.Element}
 * @constructor
 */
const Loading = () => {
    return (
        <div className="loader">
            
        </div>
    );
};

export default Loading;